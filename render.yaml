services:
  - type: web
    name: kr-stock-scanner
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: NODE_VERSION
        value: 20
