services:
  - type: web
    name: kr-stock-scanner
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
