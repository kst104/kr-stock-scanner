{
  "name": "kr-stock-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Korean stock scanner dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check dashboard-ui.js && node --check monitor.js && node --check report-scraper.js"
  },
  "engines": {
    "node": ">=20"
  }
}
